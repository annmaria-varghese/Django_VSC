# Q: Simple Django blog response

from django.http import HttpResponse

def blog(request):
    return HttpResponse("Welcome to Blog Page")

#views.py
def home(request):
    return HttpResponse("Home Page")

def about(request):
    return HttpResponse("About Store")


#urls.py
from django.urls import path
from . import views

urlpatterns = [
    path('', views.home),
    path('about/', views.about),
]